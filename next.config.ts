import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  siteUrl: 'http://localhost:3000', // 您的網站 URL
  generateRobotsTxt: true, // 是否生成 robots.txt
};

export default nextConfig;
