import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  siteUrl: 'https://comeinmacbook.com/', // 您的網站 URL
  generateRobotsTxt: true, // 是否生成 robots.txt
};

export default nextConfig;
